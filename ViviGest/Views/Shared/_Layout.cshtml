<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<!-- Bootstrap JS -->
<script src="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Mi aplicación ASP.NET</title>

    <!-- Render de los archivos CSS -->
    @Styles.Render("~/Content/css")
    <!-- Puedes agregar un enlace directo a tu archivo CSS -->
    <link href="~/Content/css/style.css" rel="stylesheet" type="text/css">

    <!-- Render de los scripts, si es necesario -->
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <!-- Navbar con fondo negro sólido -->
    <div class="navbar navbar-expand-lg navbar-dark" style="background-color: #1f1f1f;">
        <div class="container">
            <!-- Logo y nombre -->
            <a class="navbar-brand" href="#">
                ViviGest
            </a>
            <!-- Botón de menú en dispositivos móviles -->
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Elementos del menú -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Acerca de</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Servicios</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Contactenos</a>
                    </li>
                    <!-- Icono de búsqueda -->
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                            <i class="fas fa-search"></i>
                        </a>
                    </li>
                    <!-- Login -->
                    @{
                        var isLoggedIn = Session["UserID"] != null;
                    }

                    <li class="nav-item">
                        @if (isLoggedIn)
                        {
                            <a class="nav-link btn btn-danger text-white" href="@Url.Action("Logout", "Usuario")">
                                <i class="fas fa-sign-out-alt"></i> Cerrar sesión
                            </a>
                        }
                        else
                        {
                            <a class="nav-link btn btn-primary text-white" href="@Url.Action("Login", "Usuario")">
                                <i class="fas fa-sign-in-alt"></i> Login
                            </a>
                        }
                    </li>

                </ul>
            </div>
        </div>
    </div>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Mi aplicación ASP.NET</p>
        </footer>
    </div>

    <!-- Scripts -->


    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
